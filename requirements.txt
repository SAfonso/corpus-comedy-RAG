# Parsers
pymupdf                  # PDF text extraction (fallback antes de OCR)
pdf2image                # PDF → imagen para Tesseract
pytesseract              # OCR para PDFs escaneados
ebooklib                 # EPUB parsing
python-docx              # DOCX parsing

# Limpieza y detección
langdetect               # Detección de idioma

# Traducción (free tier)
deep-translator          # Wrapper DeepL free / LibreTranslate

# Google Drive
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Scheduler
APScheduler

# Testing
pytest
pytest-cov
