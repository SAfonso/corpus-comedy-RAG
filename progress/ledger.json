{
  "decisions": [
    {
      "task_id": 15,
      "decision": "reconciliacion.py es agnóstico de Supabase: recibe `candidatos` (lista de dicts id/hash_normalizado/embedding) como argumento en vez de inyectar SupabaseStore. Quién obtiene esos candidatos (qué tipo_fuente, qué alcance) queda para el caller de las tasks 16/19.",
      "reason": "mantiene el módulo 100% testeable sin red salvo la llamada de embeddings, y evita acoplar la decisión de reconciliación a un método nuevo de supabase_store.py que ningún consumidor real necesita todavía."
    },
    {
      "task_id": 15,
      "decision": "src/utils/llm/embeddings.py (stub vacío desde Fase 0) se implementó dentro de esta task, no aparte — mismo precedente que task 13 (silver.py + client.py en el mismo PR).",
      "reason": "reconciliacion.py es el primer y único consumidor; sin el cliente de embeddings la task no es verificable end-to-end."
    },
    {
      "task_id": 15,
      "decision": "UMBRAL_SIMILITUD_CAMBIADO = 0.85 como constante de módulo (no hardcodeado en la lógica de decisión).",
      "reason": "SPEC marca el umbral como indicativo/afinable con datos reales — se deja fácil de tocar sin tocar decidir_reconciliacion."
    },
    {
      "task_id": 16,
      "decision": "telegram_bot.py NO implementa la conexión real con la API de Telegram (polling/webhook, dependencia python-telegram-bot). procesar_mensaje_telegram recibe el `update` ya como dict con la forma del JSON de la Bot API — el transporte real queda para un futuro script de orquestación del flujo.",
      "reason": "el título de la task 16 solo pide Bronze/idempotencia/pre-limpieza, no conectividad; mismo patrón ya visto en el repo: scripts/run_pipeline.py (orquestación de Flujo A) sigue vacío pese a que todas las tareas de parsing/limpieza de Flujo A están done — la conectividad/orquestación se trata como entregable aparte, no atado a una task atómica de procesamiento."
    },
    {
      "task_id": 16,
      "decision": "Se añadió la tabla chistes_telegram_bronze a schema.sql (antes solo existían chistes/chistes_revisiones/temas/tecnicas/fuentes/candidatos_taxonomia/teoria_chunks) y un método guardar_mensaje_telegram_bronze en supabase_store.py (upsert con on_conflict=telegram_update_id, ignore_duplicates=True).",
      "reason": "el SPEC de Telegram exige persistencia Bronze idempotente en Supabase (no hay ficheros locales como en Flujo A/C) — no existía ninguna tabla ni método previos para eso. Igual que las tablas de la task 12, requiere aplicación manual del schema.sql actualizado en el dashboard de Supabase; el test de integración hace skip con PGRST205 hasta entonces (comportamiento esperado, no una regresión)."
    },
    {
      "task_id": 19,
      "decision": "Delegada a un subagente en worktree aislado con modelo Opus (complejidad 'alta' según feature_list.json: diseño de prompt/juicio semántico), en paralelo a la task 21 hecha directamente en el árbol de trabajo principal. El leader revisa el diff antes de mergear.",
      "reason": "política de tiering de modelos de CLAUDE.md (alta -> modelo potente); el worktree evita colisión de checkout con la task 21 corriendo en paralelo en el mismo repo."
    },
    {
      "task_id": 21,
      "decision": "teoria_store.py es un cliente Supabase propio de src/theory/ (no reusa src/jokes/supabase_store.py) pese a acceder a las mismas tablas fuentes/teoria_chunks.",
      "reason": "regla dura de CLAUDE.md: theory/ y jokes/ nunca se importan entre sí. La duplicación de credenciales/cliente es el coste aceptado de mantener los flujos desacoplados."
    },
    {
      "task_id": 21,
      "decision": "Un chunk de teoria_chunks = un FRAGMENTO ya producido por LanguageNormalizer/SubtypeDetector (no se re-divide el texto por tamaño de token). Se añadió chunk_index a teoria_chunks (antes no existía) + unique(doc_id, version_corpus, chunk_index) para idempotencia de reingesta, con upsert ON CONFLICT DO NOTHING (mismo patrón que Bronze de Telegram, task 16).",
      "reason": "un fragmento ya es la unidad semántica más fina que produce la cadena de Flujo A; partir más fino no está pedido por el SPEC y sería una decisión de tamaño de chunk arbitraria (BISTURÍ: mínimo viable). v{N} es inmutable, así que reingestar la misma versión debe ser un no-op determinista."
    },
    {
      "task_id": 21,
      "decision": "No se parsea el YAML frontmatter de vuelta (no se añade pyyaml): teoria_chunks no tiene columnas de subtipo/idioma por fragmento, solo tipo_fuente/licencia que ya están en manifest.json. _separar_cuerpo solo separa cabecera/cuerpo por el delimitador '---' fijo que emite render_document, sin parsear el contenido YAML en sí.",
      "reason": "format_normalizer.py (task 11) documentó explícitamente que parsear YAML de vuelta sería una decisión de stack aparte si hiciera falta — no hace falta para el esquema real de teoria_chunks, así que no se toma esa decisión todavía."
    },
    {
      "task_id": 19,
      "decision": "El LLM devuelve el fragmento LITERAL de texto donde empieza el setup (texto_inicio_setup), no un offset numérico de carácter. Si el fragmento no se localiza en la ventana candidata (alucinación), fallback conservador sin reintentar: se usa la ventana completa y se marca inicio_localizado=False como señal para revisión humana muestral, sin descartar el chiste ni abortar el documento.",
      "reason": "un offset numérico es casi imposible de validar (cualquier entero en rango 'parece' válido); un fragmento literal es verificable barato (str.find, con tolerancia a espaciado). El material histórico es sagrado — ante una respuesta mala del LLM, perder contenido real es peor que incluir de más una cola de intro."
    },
    {
      "task_id": 19,
      "decision": "El fin de cada chiste (nº de chistes = nº de [REMATE]) se detecta por regex determinista, NO por el LLM; el LLM solo decide el inicio del setup dentro de la ventana entre el remate anterior y este. [CHISTOIDE] se conserva como metadato de estructura, nunca como frontera.",
      "reason": "coincide exactamente con el contrato de src/jokes/historico/SPEC.md §Segmentador: el [REMATE] es determinista por diseño de marcar_remates.py (task 17), solo el arranque del setup es juicio semántico sin ancla externa (motivo por el que este paso, a diferencia de Taxonomías/Reconciliación, no lleva loop P16)."
    },
    {
      "task_id": 22,
      "decision": "processed_files.json se trata como el contrato EXTERNO de DriveMonitor (mismo formato JSON {nombre: {md5}}), no como su API privada: pipeline.py lee/revierte/escribe ese fichero directamente en vez de tocar drive_monitor.py. Tras cada DriveMonitor.scan() (que ya persiste hashes de forma prematura, antes de procesar), pipeline.py revierte el fichero al último estado realmente comprometido y solo re-confirma los hashes de los ficheros que completaron la cadena entera con éxito.",
      "reason": "la tarea prohibía tocar drive_monitor.py, pero su scan() marca 'visto' en el momento de detección, no de finalización — sin esta reconciliación, un fallo a mitad de cadena habría dejado el fichero marcado como procesado sin haberlo estado nunca, rompiendo el criterio de reanudación de src/theory/SPEC.md."
    },
    {
      "task_id": 22,
      "decision": "Un run de run_pipeline procesa el batch completo de ficheros pendientes y llama a generar_version UNA VEZ con todos los documentos que superaron la cadena — no una versión v{N} por fichero. Si generar_version falla, ningún fichero del batch se marca (todos se reintentan en el siguiente run); los ficheros que fallaron en etapas anteriores (Parser/Cleaner/Language) tampoco se incluyen en la llamada y quedan pendientes de forma independiente.",
      "reason": "coincide con 'Flujo batch, 100% determinista' de src/theory/SPEC.md y evita crear una v{N} por cada fichero individual (ruido de versionado); generar_version ya tolera reintentar una versión no finalizada sin corromper nada (ver su propio docstring), así que un fallo ahí no deja estado a medias."
    },
    {
      "task_id": 22,
      "decision": "tipo_fuente se infiere de la extensión del fichero de origen (.txt WhisperX -> transcripcion_curso, .pdf/.docx/.epub -> teoria) y `fuente` se deriva del nombre de fichero (stem, guiones/guiones bajos a espacios) — placeholders documentados en pipeline.py hasta que la integración real de Drive (P18, diferida) aporte metadatos propios (título, autor) por documento.",
      "reason": "ningún componente previo de la cadena produce estos metadatos de documento (format_normalizer.DocumentoEntrada los exige explícitos), y hoy no hay ninguna otra fuente de verdad disponible sin reactivar Drive real, fuera de scope de esta tarea."
    },
    {
      "task_id": 24,
      "decision": "Spec (no codigo) del metodo SupabaseStore.listar_candidatos_reconciliacion(tipo_fuente: str | Sequence[str]) -> list[dict], que cierra el hueco de la task 15 (reconciliacion.py recibe `candidatos` como argumento pero nada sabia obtenerlos). Devuelve una entrada por fila de `chistes` del alcance de tipo_fuente pedido, con EXACTAMENTE las tres claves que decidir_reconciliacion consume: `id` (uuid str, obligatoria), `hash_normalizado` (str|None) y `embedding` (list[float]|None) — select de esas 3 columnas, no select(*). El embedding se devuelve como list[float] (parseado desde la representacion text de pgvector si hace falta) porque similitud_coseno itera floats. El caller decide el alcance (tipo_fuente): Flujo B y C comparan hoy contra propio_historico; aceptar una secuencia deja abierto un futuro propio* sin cambiar la firma. NO filtra por version (cada fila de chistes ya es el contenido vigente; las revisiones viven en chistes_revisiones) y SI incluye variantes (chiste_origen_id no nulo = chiste distinto). Sin auto-exclusion: en reproceso, hash-match con la propia fila insertada -> IGUAL -> dedup, que es la idempotencia deseada. N de decision asignado: P20 (P19 lo tomo una tarea concurrente del Flujo C, Drive real).",
      "reason": "Se eligio traer todas las filas del tipo_fuente y comparar en Python (hash -> coseno) en vez de una query ANN nativa de pgvector (ORDER BY embedding <-> :x LIMIT K): la ANN necesita el embedding entrante en el momento del fetch, pero reconciliar_chiste (task 15, congelada) obtiene los candidatos ANTES de calcular ese embedding, asi que forzar la ANN romperia el orden hash-primero o exigiria tocar la interfaz de decidir_reconciliacion (prohibido). El volumen del corpus es bajo (GraphRAG descartado por lo mismo). La ANN queda como optimizacion futura compatible con la interfaz (puede hacer el trabajo en SQL y aun asi devolver list[dict])."
    },
    {
      "task_id": 29,
      "decision": "Flujo C SÍ activa Drive real (dirección OPUESTA a P18 de teoría, que lo difiere): componente nuevo `src/jokes/historico/drive_source.py` con contrato `DriveSource(folder_id, staging_dir, state_path, credentials_path=None).sync() -> list[Path]` (simétrico a `Loader`), que lista la carpeta Drive, descarga a staging local solo los .docx nuevos/modificados y devuelve sus paths locales para `marcar_remates.procesar_docx(...)` SIN tocar su firma. Idempotencia de Drive por metadata `fileId` + `modifiedTime` (no MD5: modifiedTime se lee sin descargar, evita la descarga misma). Idempotencia EN CAPAS e INDEPENDIENTE: Drive (qué .docx bajar) / marcar_remates (--no-sobrescribir) / Loader (MD5 del .md), cada una con su propio state_path, ninguna asume el estado de otra. Google Docs nativos se exportan a .docx (conserva w:color de run que necesita marcar_remates; export a md/txt lo perdería).",
      "reason": "el histórico crece en Drive y su run será semanal y desatendido (GitHub Actions, task 31) — no hay nadie que copie .docx a mano como sí ocurre con los libros ya descargados de teoría (P18). El envoltorio por fuera de marcar_remates/Loader respeta que ambos son contratos ya aprobados (tasks 17/18) y no se tocan."
    },
    {
      "task_id": 29,
      "decision": "Variable de entorno propia para el folder ID del histórico: `DRIVE_FOLDER_ID_HISTORICO`, separada de `DRIVE_FOLDER_ID` (carpeta de teoría). Las credenciales SÍ se comparten: misma cuenta de servicio vía `GOOGLE_APPLICATION_CREDENTIALS` (scope drive.readonly), auth desatendida compatible con CI (nunca OAuth interactivo). P asignado: P19.",
      "reason": "son carpetas de Drive distintas (confundirlas si comparten variable), pero un solo service account del mismo proyecto Google puede leer ambas; separar solo el folder ID es el mínimo necesario. La restricción de auth desatendida viene de la task 31 (trigger semanal en runner de GitHub Actions)."
    },
    {
      "task_id": 30,
      "decision": "Patrón de inyección de dependencias de DriveSource: se añadió un quinto parámetro opcional `service=None` al constructor (además de los 4 exactos fijados por la spec de la task 29: folder_id, staging_dir, state_path, credentials_path), que permite inyectar en tests un doble del cliente de `googleapiclient.discovery.build('drive','v3',...)` con la misma interfaz mínima (`files().list/get_media/export`). En producción nunca se pasa: el cliente real se construye de forma perezosa (solo en el primer `sync()`) a partir de `credentials_path`/`GOOGLE_APPLICATION_CREDENTIALS` vía `google.oauth2.service_account.Credentials.from_service_account_file`.",
      "reason": "la spec fija la firma de 4 parámetros y no debía rediseñarse, pero el propio encargo de la task 30 permitía elegir el patrón de DI ('inyecta el cliente ya construido como parámetro si eso hace la clase más testeable'); un 5º parámetro con default None no rompe ninguna llamada que use solo los 4 documentados y evita mockear `googleapiclient.discovery.build` a nivel de módulo (más frágil) o tocar la red en los tests."
    },
    {
      "task_id": 26,
      "decision": "coste.py IMPORTA directamente `segmentador._construir_ventanas`/`_build_prompt` y `silver._build_prompt`/`_limpiar_marcado_historico` (funciones con guion bajo) en vez de duplicar la construcción del prompt para el dry-run. Proxy para Silver: nº de llamadas ≈ nº de `[REMATE]` (ventanas candidatas), y el TEXTO de cada prompt de Silver usa la ventana candidata COMPLETA (no el recorte real, que solo se conoce llamando de verdad al LLM del Segmentador) — sobreestimación deliberada. Conteo de tokens: por defecto `contar_tokens_oficial` usa `client.models.count_tokens(...)` del SDK `google-genai` (confirmado inspeccionando el paquete instalado, 1.67.0 — golpea el endpoint `countTokens`, de conteo, no de generación; requiere red+credenciales pero no genera contenido), con import perezoso igual que `generar_json`. Alternativa 100% offline `estimar_tokens_heuristica` (~4 caracteres/token, margen ±20-30% documentado) para entornos sin red/credenciales. Ambos inyectables vía `contar_tokens=` (mismo patrón que `llamar_llm`); los tests unitarios siempre inyectan un fake. Gate (`evaluar_gate`) compara tokens_totales contra `UMBRAL_TOKENS_DEFAULT=1_000_000` (parámetro > env `HISTORICO_COSTE_MAX_TOKENS` > default), con un umbral en € opcional (`HISTORICO_COSTE_MAX_EUR`) que solo participa si se configura explícitamente, porque el precio €/millón de tokens (`PRECIO_EUR_POR_MILLON_TOKENS_DEFAULT=0.10`, también configurable) es best-effort (el SDK no expone precios en vivo). `evaluar_gate` nunca hace `sys.exit`/raise: devuelve `DecisionGate(permitir, razon, ...)` para que el futuro `scripts/run_historico.py` (task 28) decida el exit code.",
      "reason": "precisión sobre encapsulamiento: una copia duplicada del prompt divergiría en silencio del prompt real de producción; importar la función real rompe la importación/tests en cuanto cambie la firma, en vez de mentir sobre el coste calladamente. El umbral en tokens (magnitud estable) es la salvaguarda por defecto porque el precio en € depende de un modelo/tier que cambia fuera del control de este módulo; frenar por volumen evita que un bug de idempotencia dispare un run completo sin depender de tener el precio exacto vigente."
    },
    {
      "task_id": 27,
      "decision": "El gate de coste (`historico/coste.py`, task 26) NO va dentro de `run_historico_pipeline`: es un dry-run PREVIO que su propio docstring sitúa antes del run completo, invocado por `scripts/run_historico.py` (task 28, `--dry-run` + exit code 2 'abort-por-coste'). El título de la task 27 y la lista de etapas 0-5 del SPEC (§'Entrada y etapas') no incluyen el gate; el pipeline asume que la decisión de gastar ya se tomó aguas arriba. Reanudación por DOCUMENTO: no se inventa una cuarta capa de estado — se apoya en la idempotencia en capas ya existente (DriveSource fileId+modifiedTime, marcar_remates existencia del .md, Loader MD5) y se resuelve el único hueco que ninguna capa cubre sola (fallo a mitad de documento) replicando el patrón de Flujo A (`theory/pipeline.py`): se carga `estado_comprometido` del `loader_state_path`, se llama a `Loader.load()` (que escribe el MD5 prematuramente), se REVIERTE el fichero a `estado_comprometido`, se procesa cada documento en try/except independiente y solo se compromete su MD5 (ya calculado por el Loader, sin recalcular) si TODOS sus chistes se rutean con éxito. La unidad atómica es el CHISTE (segmentar→Silver→taxonomía→reconciliación→routing); si un documento con N chistes falla en el k-ésimo, el documento entero se reintenta en el siguiente run y los chistes ya insertados deduplican por hash (decisión IGUAL, §Reconciliación 'Sin auto-exclusión') — no se reinsertan, así que no hace falta transacción multi-chiste. DriveSource conserva su semántica propia (commitea modifiedTime al descargar, como DriveMonitor de Flujo A): un .docx cuyo marcado falle por round-trip no se re-descarga hasta que cambie en Drive — correcto, porque un fallo de round-trip es determinista y señala un .docx fuente a corregir (cambiaría su modifiedTime); se reporta en `marcado_fallidos`, no se oculta. Routing etapa 5: IGUAL=dedup sin escribir; NUEVO=`crear_chiste`(version_actual=1)+`crear_revision`(v1); CAMBIADO=`crear_revision`(version_actual previa +1, append-only)+`actualizar_chiste`(apunta version_actual a la nueva y refresca texto/hash/embedding/estado). Los tema_id/tecnica_id resueltos por `resolver_taxonomia` solo se escriben si hubo match — en CAMBIADO nunca se sobrescribe un ID existente con None cuando la resolución encoló un candidato para revisión humana. `estructura_detectada` (string libre de Silver) se empaqueta en el jsonb de la revisión como {descripcion, tecnica_id}. tipo_fuente='propio_historico'. Inyección de dependencias (mismo patrón que `scripts/run_pipeline.py` de Flujo A): `drive_source`, `store` y las funciones de etapa (`procesar_docx_fn`/`segmentar_fn`/`estructurar_fn`/`resolver_taxonomia_fn`/`reconciliar_fn`) son inyectables; `Loader` y `marcar_remates` (puros, sin red) se usan reales tanto en producción como en tests.",
      "reason": "Separar el gate del pipeline respeta el reparto que el propio coste.py declara ('NO ejecuta el run — solo estima y decide') y deja al caller (task 28) la decisión de exit code, sin acoplar una política de presupuesto dentro del orquestador. Apoyarse en la idempotencia en capas existente en vez de una cuarta capa evita duplicar estado y mantener dos fuentes de verdad; el patrón revert-premature-write de Flujo A garantiza que un documento solo desaparece del Loader si completó su cadena entera, y el dedup por hash de la reconciliación hace el replay idempotente sin necesidad de transacciones distribuidas contra Supabase."
    },
    {
      "task_id": 28,
      "decision": "scripts/run_historico.py construye él mismo `documentos: list[{name,content}]` para el gate ejecutando las etapas 0-2 (DriveSource.sync() → marcar_remates.procesar_docx → Loader.load()) ANTES de llamar a `run_historico_pipeline`, que repite esas mismas etapas por dentro si el gate permite. Gotcha descubierto: `Loader.load()` no es idempotente desde fuera de pipeline.py — escribe su MD5 en `state_path` en cuanto LEE un documento, no cuando termina de procesarlo, así que una segunda llamada (la interna de `run_historico_pipeline`) vería el MD5 ya registrado y devolvería lista vacía: el gate diría 'adelante' y el run real no procesaría nada, fallo silencioso. Solución: se reutiliza la MISMA instancia de `drive_source` entre el paso de gate y la llamada real (evita doble sync real y conserva `marcado_fallidos`), y el estado del Loader se revierte tras leerlo en el paso de gate — mismo patrón revert-premature-write que ya usa `run_historico_pipeline` internamente (task 27) para su propio problema equivalente. Exit codes fieles al título de la tarea (3 códigos, no 4 como run_pipeline.py de Flujo A): 0=éxito (incluye --dry-run con gate en verde), 1=fallo (documento/.docx fallido en el run real o excepción inesperada), 2=abort por coste (gate bloquea, con o sin --dry-run — nunca se invoca el pipeline). `--dry-run` reserva su exit code exclusivamente a la decisión del gate: un `.docx` que falla el marcado durante el paso de gate no reaparece en el run real (el segundo sync() no lo re-stagea), así que `marcado_fallidos_gate` se reporta siempre en el JSON pero solo pliega en el exit code cuando se ejecuta el run real.",
      "reason": "coste.py y pipeline.py son de tareas ya cerradas (26/27) y no se modificaron — la alternativa de tocar Loader.load() para hacerlo idempotente habría reabierto el scope de una tarea aprobada por un problema que solo existe en el caller. Reutilizar la misma instancia de drive_source y revertir el estado del Loader resuelve el gotcha sin duplicar red real ni estado persistente nuevo, y es el mismo patrón ya validado en pipeline.py (task 27) para su propio problema de escritura prematura, en vez de inventar un mecanismo distinto."
    },
    {
      "task_id": 31,
      "decision": "`.github/workflows/run_historico_semanal.yml` (primer workflow del repo): wiring puro sobre `scripts/run_historico.py` (task 28), cero lógica reimplementada. Cron `0 3 * * 6` (sábado 03:00 UTC) — fuera de horas pico de Drive/LLM y deja el domingo entero de margen para investigar un fallo antes del lunes laboral. Secrets con convención 1:1 sobre `.env.example` (DRIVE_FOLDER_ID_HISTORICO, SUPABASE_URL, SUPABASE_SERVICE_KEY, LLM_API_KEY, LLM_MODEL, EMBEDDINGS_API_KEY, EMBEDDINGS_MODEL + 3 opcionales del gate de coste) EXCEPTO la credencial de Drive: un secreto de GH Actions es un string plano, así que se usa `GOOGLE_SERVICE_ACCOUNT_JSON` (el JSON completo de la cuenta de servicio como string) en vez de `GOOGLE_APPLICATION_CREDENTIALS` (que espera una ruta de fichero); un step del workflow escribe ese string a `$RUNNER_TEMP/google-service-account.json` y exporta `GOOGLE_APPLICATION_CREDENTIALS` apuntando ahí. `workflow_dispatch` añadido como trigger manual con input opcional `dry_run` (boolean), útil para el primer run real y para probar el gate de coste sin ejecutar el pipeline completo. Exit codes 1 (fallo) y 2 (abort por coste) del CLI hacen fallar el job visiblemente en ambos casos (sin `continue-on-error`), distinguiéndose solo en el `$GITHUB_STEP_SUMMARY` (un abort por coste también requiere atención humana, así que se marca en rojo igual). Persistencia de estado entre runs: se añadió `actions/cache` sobre `data/staging/historico/` + los JSON de estado de DriveSource/Loader (rutas reales de `pipeline.py`), clave por `run_id` con `restore-keys` de prefijo para recuperar el cache más reciente — limitación documentada explícitamente (no en silencio): la cache de GH Actions es best-effort (LRU, límite 10GB/repo, sin garantía de persistencia), un cache-miss no rompe la corrección (las capas de idempotencia del propio Flujo C siguen siendo válidas) solo repite trabajo. Setup: `actions/setup-python@v5` (3.12, `cache: pip`) + `pip install -r requirements.txt` directo sin `.venv` (el runner ya es un entorno aislado y efímero, a diferencia de `init.sh` local que sí necesita venv por PEP 668). Documentado como P21 en `docs/specs/00-overview.md` (línea en el changelog de cabecera + párrafo en la sección 'Idempotencia y versionado', inmediatamente después de P20) — cierra explícitamente la promesa que P19 ya dejó anotada ('ejecución semanal y desatendida, GitHub Actions, task 31').",
      "reason": "Secret separado para las credenciales de Drive (en vez de intentar forzar GOOGLE_APPLICATION_CREDENTIALS como secreto) porque GH Actions no soporta secrets tipo fichero nativamente, y escribirlo a disco en runtime es el patrón estándar recomendado para credenciales JSON en runners efímeros. El cron en sábado de madrugada prioriza no competir con tráfico ni bloquear el arranque de la semana si algo sale mal. Fallar visiblemente en exit code 1 Y 2 (en vez de tratar el abort-por-coste como 'éxito parcial') respeta el espíritu del gate de la task 26: un abort por presupuesto es una señal que alguien debe revisar (¿subió el volumen de golpe? ¿hay un bug de idempotencia disparando reprocesos?), no un resultado silenciosamente aceptable. La cache de estado se añadió pese a no estar en el título literal de la tarea porque sin ella el cron 'semanal' repetiría descargas/marcado desde cero cada vez (correcto pero ineficiente y más caro en llamadas a Drive/LLM del lado del gate) — se documentó como best-effort en vez de prometer persistencia garantizada que GH Actions no ofrece."
    }
  ],
  "tasks": [
    {
      "id": 15,
      "title": "Contrato B/C: reconciliacion.py — dedup híbrido hash + embedding (IGUAL/CAMBIADO/NUEVO)",
      "status": "done",
      "summary": "Implementados src/jokes/reconciliacion.py (calcular_hash_normalizado, similitud_coseno, decidir_reconciliacion, reconciliar_chiste) y src/utils/llm/embeddings.py (generar_embedding vía Gemini, antes vacío). 33 tests unitarios nuevos (0 red) + 1 test de integración real contra Gemini (embeddings), todos en verde. Sin cambios en supabase_store.py — ver decisión de scope arriba. Branch task/15-reconciliacion.",
      "rejections": 0
    },
    {
      "id": 16,
      "title": "Flujo B: telegram_bot.py — Bronze inmutable, idempotencia por telegram_update_id, pre-limpieza mínima",
      "status": "done",
      "summary": "Implementado src/jokes/telegram/telegram_bot.py (limpiar_texto_telegram, _extraer_datos_mensaje, procesar_mensaje_telegram) contra un `update` con la forma del JSON de la Bot API de Telegram, sin dependencia del SDK. Añadidos chistes_telegram_bronze a schema.sql y guardar_mensaje_telegram_bronze a supabase_store.py (upsert idempotente, ON CONFLICT DO NOTHING real). 40 tests unitarios nuevos (0 red) + 1 test de integración (skip esperado: tabla nueva, pendiente de aplicar schema.sql en Supabase). Conectividad real con Telegram fuera de scope — ver decisión arriba. Branch task/16-telegram-bot.",
      "rejections": 0
    },
    {
      "id": 21,
      "title": "Ingesta de /data/processed/v{N}/ a Supabase (teoria_chunks + índice pgvector compartido)",
      "status": "done",
      "summary": "Implementados src/theory/teoria_store.py (cliente Supabase propio de teoría, sin importar src/jokes/) y src/theory/ingest_teoria.py (ingestar_version: lee manifest.json + .txt de v{N}/, un chunk por fragmento, resuelve/crea fuente, embedding vía src/utils/llm/embeddings.py ya existente de la task 15, upsert idempotente). Añadidos chunk_index + unique(doc_id, version_corpus, chunk_index) a teoria_chunks en schema.sql. 14 tests unitarios nuevos (cadena real de Flujo A -> v1 real en tmp_path, sin red) + 1 test de integración (skip esperado: columna nueva pendiente de aplicar en Supabase, PGRST204 documentado en docs/specs/KNOWN_ERRORS_GLOBAL.md — hallazgo nuevo). 308/308 tests unitarios del repo en verde. Branch task/21-ingesta-teoria.",
      "rejections": 0
    },
    {
      "id": 19,
      "title": "Flujo C: segmentador.py — diseño del prompt de inicio de setup (juicio semántico, sin loop, revisión humana muestral)",
      "status": "done",
      "summary": "Delegada a subagente Opus en worktree aislado (complejidad alta). Implementado src/jokes/historico/segmentador.py: _construir_ventanas (frontera por [REMATE], determinista, regex pura) + segmentar_documento (una llamada LLM por remate, sin loop P16, localiza el inicio literal del setup con fallback conservador ante alucinación — ver decisiones arriba). 29 tests unitarios (fixtures reales de Freskito-Informático.md) + 1 test de integración real contra Gemini, todos en verde. Revisado por el leader: branch basada en main desactualizado (previo al merge de la task 21) pero sin overlap de ficheros — merge limpio verificado localmente (337/337 unit tests, 11 passed/3 skipped integración) antes de mergear. Branch task/19-segmentador. Backlog completo: 21/21.",
      "rejections": 0
    },
    {
      "id": 22,
      "title": "Flujo A: theory/pipeline.py — módulo orquestador importable (DriveMonitor→Parser→SubtypeDetector→Cleaner→Language→QualityScorer→generar_version), reanudación por processed_files.json",
      "status": "done",
      "summary": "Implementado src/theory/pipeline.py: run_pipeline encadena DriveMonitor -> Parser (dispatch por extensión: .txt whisperx, .pdf/.docx/.epub markitdown) -> SubtypeDetector -> Cleaner -> LanguageNormalizer -> generar_version (QualityScorer ya embebido en generar_version, no se repite). Fricción resuelta: DriveMonitor.scan() persiste el hash MD5 de un fichero en processed_files.json en cuanto lo detecta pendiente, no cuando termina de procesarse — pipeline.py revierte esa escritura prematura (mismo fichero JSON, contrato externo documentado, sin tocar drive_monitor.py) y solo confirma el hash de cada fichero tras completar la cadena entera con éxito hasta generar_version; si generar_version falla, ningún fichero del batch se marca. 11 tests unitarios nuevos con fixtures reales (sample_transcript.txt/.pdf): cadena feliz, fallo a mitad de cadena no marca el fichero, reanudación tras fallo previo, fallo en generar_version no marca nada del batch, ficheros sin Parser conocido (.gitkeep) ignorados sin reintento. 346/346 tests unitarios en verde (2 errores preexistentes ajenos: datos privados no presentes en el worktree). Branch task/22-theory-pipeline-orquestador.",
      "rejections": 0
    },
    {
      "id": 29,
      "title": "Flujo C: spec de integración Drive real (jokes/historico/SPEC.md + nota en 00-overview.md) — fuente pasa de .docx local a carpeta Drive real, descarga a staging local, marcar_remates conserva firma local",
      "status": "done",
      "summary": "Tarea de SPEC (sin código). Documentado en src/jokes/historico/SPEC.md el nuevo contrato de entrada del Flujo C: nueva §Fuente de entrada — carpeta Drive real (P19) con el contrato de `drive_source.py` (implementación en task 30), idempotencia por metadata de Drive (fileId+modifiedTime) e idempotencia EN CAPAS independiente (Drive/marcar_remates/Loader). Etapas renumeradas (DriveSource=etapa 0), marcar_remates.procesar_docx y Loader intactos. Añadida decisión P19 en docs/specs/00-overview.md (header + comparativa de idempotencia). .env.example: nueva variable DRIVE_FOLDER_ID_HISTORICO (credenciales GOOGLE_APPLICATION_CREDENTIALS compartidas con teoría, service account, scope drive.readonly, auth desatendida para CI). Contrato exacto para la task 30: DriveSource(folder_id, staging_dir, state_path, credentials_path=None).sync()->list[Path]. Sin tocar marcar_remates.py/loader.py; Flujo A P18 intacto. 63/63 tests de Flujo C en verde (los 8 errores de colección del suite completo son deps ausentes en el worktree: markitdown, preexistentes y ajenos). Branch task/29-spec-drive-historico, PR #19 merged a main.",
      "rejections": 0
    },
    {
      "id": 24,
      "title": "Contrato B/C: spec del metodo de SupabaseStore para obtener candidatos de reconciliacion filtrados por tipo_fuente (src/jokes/SPEC.md Reconciliacion)",
      "status": "done",
      "summary": "Tarea SOLO de spec (complejidad alta, decision de arquitectura del contrato compartido B/C; cero codigo de produccion, sin tests porque no hay codigo). Ampliada Reconciliacion de src/jokes/SPEC.md con una subseccion nueva que documenta SupabaseStore.listar_candidatos_reconciliacion(tipo_fuente: str | Sequence[str]) -> list[dict]: firma, las 3 claves exactas del retorno (id/hash_normalizado/embedding) verificadas linea a linea contra decidir_reconciliacion/reconciliar_chiste, embedding como list[float], que NO filtra (sin filtro de version, variantes incluidas, sin auto-exclusion) y el trade-off traer-todo-vs-ANN-pgvector. Anadida la decision P20 al header y cuerpo de docs/specs/00-overview.md. Implementacion diferida a la task 25 (dependiente). No se toco reconciliacion.py ni ninguna pieza aprobada. N P20 en vez de P19 porque P19 lo tomo una tarea concurrente del Flujo C (Drive real, tasks 29/30). Branch task/24-spec-candidatos-reconciliacion.",
      "rejections": 0
    },
    {
      "id": 25,
      "title": "Contrato B/C: implementar en supabase_store.py el metodo de fetch de candidatos definido en la task 24",
      "status": "done",
      "summary": "Implementado SupabaseStore.listar_candidatos_reconciliacion(tipo_fuente: str | Sequence[str]) -> list[dict] en src/jokes/supabase_store.py, firma y forma de retorno identicas a la spec de la task 24 (select('id, hash_normalizado, embedding'), sin filtro de version, con variantes). Dos helpers puros nuevos: _normalizar_tipo_fuente_candidatos (str o Sequence[str], reutiliza _validar_tipo_fuente_chiste por cada valor, ValueError si esta fuera de enum o la secuencia esta vacia) y _parsear_embedding (adapta list[float] ya deserializada o el text serializado de pgvector -- '[0.1,0.2,0.3]' -- a list[float], o None si la columna esta vacia; unico punto de adaptacion pgvector<->reconciliacion.py). Tests unitarios con un doble de cliente en memoria (_FakeClienteChistes/_FakeConsultaChistes, sin red) verificando: filtro por tipo_fuente unico (.eq) y por secuencia (.in_), las 3 claves exactas en el retorno (nunca select('*')), inclusion de variantes con chiste_origen_id, parseo de embedding texto/lista/None, y rechazo de tipo_fuente invalido; mas tests directos de los dos helpers puros. 218/218 tests de tests/unit/jokes/ en verde; suite completa de tests/unit/ en verde salvo los mismos 8 errores de coleccion preexistentes (deps ausentes en el worktree: markitdown/langdetect, documentados ya en la task 29) y 2 errores preexistentes de test_whisperx_parser.py (fixtures de datos ausentes en este worktree, ajenos a jokes/). Verificado ademas con una llamada real de solo lectura contra el Supabase del proyecto (tabla chistes vacia, sin escritura). No se conecto el metodo a ningun caller (orquestacion de Flujo C es la task 27, fuera de scope) ni se toco reconciliacion.py/silver.py/schema.sql -- las columnas que la spec asume ya existian en chistes. Branch task/25-metodo-candidatos-reconciliacion.",
      "rejections": 0
    },
    {
      "id": 30,
      "title": "Flujo C: historico/drive_source.py — lista carpeta Drive real y descarga .docx nuevos/modificados a staging, idempotencia por metadata Drive (fileId+modifiedTime/md5)",
      "status": "done",
      "summary": "Implementado src/jokes/historico/drive_source.py con la clase DriveSource exactamente contra el contrato fijado por la task 29 (spec P19): DriveSource(folder_id, staging_dir, state_path, credentials_path=None).sync() -> list[Path]. Idempotencia por metadata de Drive (fileId + modifiedTime RFC3339, NO MD5), capa independiente de la del Loader, con su propio state_path JSON. .docx subidos -> files().get_media(fileId=...); Google Docs nativos -> files().export(fileId=..., mimeType=.../wordprocessingml.document) para conservar w:color de run; otros MIME filtrados server-side en la query de listado (folder_id + trashed=false + mimeType in {docx, gdoc}). Auth de cuenta de servicio (google.oauth2.service_account, GOOGLE_APPLICATION_CREDENTIALS o credentials_path, scope drive.readonly), nunca OAuth interactivo; construcción del cliente perezosa (solo en el primer sync() real). Patrón de DI elegido: parámetro adicional `service=None` en el constructor (no rompe la firma de 4 parámetros de la spec, que sigue siendo la única usada en producción) para inyectar un doble del cliente de googleapiclient.discovery.build en tests, evitando red por completo. 11 tests unitarios nuevos en tests/unit/jokes/historico/test_drive_source.py (descarga nueva, export de Google Doc nativo, idempotencia sin cambios/con modifiedTime cambiado/con rename sin cambio de contenido, paginación, scope exacto de la query, solo-lectura, fallo explícito sin credenciales, persistencia de estado entre instancias) — contenido descargado en los tests = fixture real tests/fixtures/Freskito-Informático.docx, nunca bytes inventados. No se tocó marcar_remates.py ni loader.py (contratos aprobados, tasks 17/18); no se implementó el trigger de GitHub Actions (task 31, fuera de scope). 209/209 tests unitarios en verde excluyendo los módulos con deps opcionales ausentes en este worktree (langdetect, markitdown — preexistente y ajeno a esta tarea). Sin CI configurado en el repo (.github/workflows/ no existe todavía, ver task 31); mergeStateStatus CLEAN, merge directo. Branch task/30-drive-source-historico, PR #22 merged a main.",
      "rejections": 0
    },
    {
      "id": 26,
      "title": "Flujo C: historico/coste.py — dry-run de estimación de tokens + gate de presupuesto antes del run completo (llm-policy.md §Control de coste)",
      "status": "done",
      "summary": "Implementado src/jokes/historico/coste.py: reconstrucción sin red de los prompts de Segmentador (uno por ventana candidata, vía `_construir_ventanas`/`_build_prompt` importados de segmentador.py) y de Silver (proxy: uno por `[REMATE]`, usando la ventana completa limpia como texto conservador, vía `_build_prompt`/`_limpiar_marcado_historico` importados de silver.py — ver decisión arriba). `estimar_coste` agrega tokens/€ sobre `list[{name, content}]` (misma forma que `Loader.load()`) con `contar_tokens` inyectable (default `contar_tokens_oficial`, que usa `client.models.count_tokens` real del SDK `google-genai`; alternativa 100% offline `estimar_tokens_heuristica`). `evaluar_gate`/`estimar_y_evaluar` devuelven una `DecisionGate(permitir, razon, estimacion, umbral_tokens, umbral_eur)` sin ejecutar ningún control de flujo (ni sys.exit ni raise) — el exit code queda para `scripts/run_historico.py` (task 28). 25 tests unitarios nuevos (0 red, contador fake inyectado, fixture real Freskito-Informático.md ya existente) + 1 test de integración contra `count_tokens` real de Gemini (con skip si faltan credenciales, igual que test_segmentador_live.py/test_silver_live.py) — pasó en real en este entorno. Nuevas variables documentadas en .env.example: HISTORICO_COSTE_MAX_TOKENS, HISTORICO_COSTE_MAX_EUR, HISTORICO_COSTE_EUR_POR_MILLON_TOKENS. No se tocó segmentador.py/silver.py/loader.py. 251/251 tests unitarios relevantes en verde (8 errores de colección preexistentes y ajenos en tests/unit/theory por deps ausentes en el worktree: langdetect/markitdown, confirmado idéntico en main antes de este cambio). Branch task/26-historico-coste-gate.",
      "rejections": 0
    },
    {
      "id": 23,
      "title": "Flujo A: scripts/run_pipeline.py — CLI estable para consumidor externo (RAG por subprocess): invoca pipeline.py, --ingest→ingestar_version, exit codes fiables + resumen JSON",
      "status": "done",
      "summary": "Implementado scripts/run_pipeline.py: wiring puro sobre src/theory/pipeline.run_pipeline (task 22) y src/theory/ingest_teoria.ingestar_version (task 21) ya aprobados, sin reimplementar lógica. Resumen JSON (construir_resumen) por stdout por defecto o a fichero con --summary-out (único contenido de stdout; logs de progreso siempre a stderr, para que un consumidor externo por subprocess pueda hacer json.loads(stdout) sin filtrar nada). Exit codes: 0 éxito (incluye 'nada pendiente', no es fallo), 1 algún fichero en fallidos (prioridad sobre fallo de --ingest), 2 --ingest lanzó excepción con pipeline sin fallidos, 3 fallo fatal inesperado de run_pipeline (red de seguridad, nunca traceback crudo al consumidor externo). --ingest es no-op sin error si version_dir es None (no se ingesta una versión antigua); TeoriaStore solo se instancia (credenciales Supabase) si --ingest está presente. 15 tests unitarios nuevos (tests/unit/scripts/test_run_pipeline_cli.py) inyectando run_pipeline_fn/ingestar_version_fn/crear_store_fn de mentira, sin red. 372/374 tests unitarios en verde (2 errores preexistentes ajenos: fixtures de datos privados de test_whisperx_parser.py no presentes en este worktree, mismo hallazgo ya documentado en la task 22). Branch worktree-agent-a019778c3971e5883.",
      "rejections": 0
    },
    {
      "id": 27,
      "title": "Flujo C: historico/pipeline.py — módulo orquestador importable (drive_source→marcar_remates→Loader→Segmentador→Silver→resolver_taxonomia→reconciliar_chiste→routing IGUAL/CAMBIADO/NUEVO a Supabase), reanudación por documento",
      "status": "done",
      "summary": "Implementado `src/jokes/historico/pipeline.py` con la función pública `run_historico_pipeline`: wiring puro sobre los componentes ya aprobados del Flujo C (DriveSource task 30, marcar_remates task 17, Loader task 18, segmentar_documento task 19, estructurar_chiste/Silver task 13, resolver_taxonomia task 14, reconciliar_chiste task 15, SupabaseStore + listar_candidatos_reconciliacion task 25/12), sin reimplementar ninguna etapa. Encadena las etapas 0-5 del SPEC (§'Entrada y etapas'): DriveSource.sync()→procesar_docx→Loader.load()→segmentar_documento→estructurar_chiste→resolver_taxonomia(tema+técnica)→reconciliar_chiste→routing a Supabase con tipo_fuente='propio_historico'. Routing etapa 5: IGUAL=dedup (no escribe), NUEVO=crear_chiste(v1)+crear_revision(v1), CAMBIADO=crear_revision(siguiente versión)+actualizar_chiste (apunta version_actual, refresca texto/hash/embedding/estado; tema_id/tecnica_id solo si hubo match, nunca sobrescribe un ID existente con None). Reanudación POR DOCUMENTO replicando el patrón de Flujo A (theory/pipeline.py): se apoya en la idempotencia en capas ya existente (DriveSource fileId+modifiedTime, Loader MD5) sin inventar una cuarta capa; carga estado comprometido del loader_state_path, llama a Loader.load() (que escribe MD5 prematuramente), revierte el fichero, procesa cada documento en try/except independiente y solo compromete su MD5 si TODOS sus chistes se rutean con éxito. Fallo a mitad de documento: se reintenta el documento entero en el siguiente run y los chistes ya insertados deduplican por hash (IGUAL) — ningún chiste a medias en Supabase, sin transacción multi-chiste. El gate de coste (coste.py, task 26) NO va aquí: es un dry-run previo del caller (task 28). Dependencias inyectables (drive_source, store y las 5 funciones de etapa) para testear sin red; Loader y marcar_remates (puros) se usan reales. 15 tests unitarios nuevos (tests/unit/jokes/historico/test_pipeline.py) con fixtures reales (Freskito-Informático.md/.docx): routing IGUAL/CAMBIADO/NUEVO, reanudación (doc completado no se reprocesa, doc modificado sí, fallo a mitad no compromete y reintenta, 2º chiste falla→1º deduplica en reproceso), cadena completa desde .docx real con marcar_remates real, fallo de round-trip reportado sin abortar el batch, y casos borde. 430/432 tests unitarios en verde (los 2 errores restantes son preexistentes y ajenos: fixtures de datos privados de test_whisperx_parser.py no presentes en este worktree, mismo hallazgo ya documentado en las tasks 22/23). tests/integration colecciona sin errores (15 tests). Ningún módulo encadenado modificado (scope respetado); theory/ no se importa. Rama task/27-historico-pipeline-orquestador.",
      "rejections": 0
    },
    {
      "id": 28,
      "title": "Flujo C: scripts/run_historico.py — CLI estable para consumidor externo (RAG por subprocess): ejecuta gate de la task 26 (--dry-run), luego pipeline.py; exit codes 0/1/2 (éxito/fallo/abort-por-coste) + resumen JSON",
      "status": "done",
      "summary": "Implementado scripts/run_historico.py (antes vacío, stub de Fase 0): wiring puro sobre historico.coste.estimar_y_evaluar (task 26) y historico.pipeline.run_historico_pipeline (task 27), sin tocar ninguno de los dos. Mismo contrato de proceso que scripts/run_pipeline.py (task 23): JSON de resumen por stdout o --summary-out, logs de progreso a stderr, funciones inyectables. Exit codes fieles al título (3, no 4 como Flujo A): 0 éxito (incluye --dry-run con gate en verde), 1 fallo (documento/.docx fallido en el run real o excepción inesperada), 2 abort por coste (gate bloquea, con o sin --dry-run — nunca invoca el pipeline). Reto resuelto: construir `documentos: list[{name,content}]` para el gate sin duplicar lógica de pipeline.py — se ejecutan las etapas 0-2 (DriveSource.sync()→marcar_remates.procesar_docx→Loader.load()) en el propio CLI antes del gate; si permite, se invoca run_historico_pipeline que repite esas etapas por dentro (idempotentes salvo el gotcha de Loader.load(), ver decisión de ledger task_id=28: se reutiliza la misma instancia de drive_source y se revierte el estado del Loader tras el paso de gate, mismo patrón revert-premature-write que pipeline.py usa internamente). --dry-run reserva su exit code exclusivamente a la decisión del gate; marcado_fallidos del paso de gate se reporta siempre en el JSON pero solo pliega en el exit code cuando corre el run real. Nueva entrada en src/jokes/historico/KNOWN_ERRORS.md documentando el gotcha de Loader.load() no idempotente desde fuera de pipeline.py. 19 tests unitarios nuevos (tests/unit/scripts/test_run_historico_cli.py) con dobles de run_historico_pipeline_fn/estimar_y_evaluar_fn/crear_drive_source_fn/procesar_docx_fn, incluido un caso con marcar_remates.procesar_docx real sobre Freskito-Informático.docx y un test específico de no-compromiso prematuro del Loader. 449 tests unitarios en verde (2 errores preexistentes ajenos, mismo hallazgo ya documentado en tasks 22/23/27: fixtures de datos privados de test_whisperx_parser.py ausentes en el worktree); tests/integration: 10 passed, 5 skipped (credenciales). 0 rechazos de FISCAL. PR #28, merge commit fa3ae84.",
      "rejections": 0
    },
    {
      "id": 31,
      "title": "Flujo C: .github/workflows/run_historico_semanal.yml — trigger schedule cron semanal, secrets de Supabase/Drive/LLM, ejecuta run_historico.py; documenta la decisión en 00-overview.md",
      "status": "done",
      "summary": "Implementado .github/workflows/run_historico_semanal.yml (primer workflow del repo): wiring puro sobre scripts/run_historico.py (task 28). Cron 0 3 * * 6 (sábado 03:00 UTC) + workflow_dispatch (input opcional dry_run). Secrets 1:1 con .env.example salvo Drive: GOOGLE_SERVICE_ACCOUNT_JSON (string) se escribe a $RUNNER_TEMP/google-service-account.json en un step previo y GOOGLE_APPLICATION_CREDENTIALS apunta ahí (GH Actions no tiene secrets tipo fichero). Exit codes 1/2 del CLI hacen fallar el job visiblemente en ambos (sin continue-on-error), distinguidos en $GITHUB_STEP_SUMMARY. actions/cache sobre data/staging/historico/ + JSON de estado de DriveSource/Loader para no repetir descargas cada semana (best-effort, documentado como limitación conocida — LRU sin garantía). actions/setup-python@v5 (3.12, cache pip) + pip install directo sin .venv (runner ya aislado). Documentado como P21 en docs/specs/00-overview.md (changelog de cabecera + párrafo tras P20) — cierra la promesa de P19. 9 tests nuevos (tests/unit/scripts/test_run_historico_workflow.py: YAML válido, triggers, sin .venv, sin continue-on-error, mecanismo de credenciales, secrets esperados, cache, no toca data/raw); PyYAML añadido a requirements.txt. 347 tests unitarios + 28/28 en tests/unit/scripts/ en verde (9 errores de colección + 2 fallos preexistentes y ajenos en theory/utils, mismo patrón ya documentado en tasks 22/23/27/28/29/30). Sin entrada nueva en KNOWN_ERRORS.md (tarea de infraestructura, nada no-obvio fuera de lo ya cubierto en P21/comentarios del YAML). No se disparó un run real (sin secrets en este entorno de dev); validado por yaml.safe_load + revisión manual. 0 rechazos de FISCAL. PR #29, merge commit 5a62e78. Última tarea del backlog — 31/31 done.",
      "rejections": 0
    },
    {
      "id": 32,
      "title": "Flujo B: spec del transporte webhook y la orquestación end-to-end (telegram/SPEC.md §Transporte + §Orquestación, P22 en 00-overview.md)",
      "status": "done",
      "summary": "Tarea de SPEC (sin código ni tests, mismo patrón que tasks 24/29). Añadidas a src/jokes/telegram/SPEC.md las secciones §Transporte — webhook (P22) (webhook vs polling con motivo; contrato HTTP fijo POST /telegram/webhook + GET /health sin auth; enum de respuesta aceptado|duplicado|ignorado_no_texto|ignorado_no_autorizado; regla dura: todo no-2xx hace reintentar a Telegram, así que las decisiones terminales responden 200 y los únicos no-2xx son 403 secret / 400 cuerpo ilegible; validación de X-Telegram-Bot-Api-Secret-Token en tiempo constante con fallo de arranque si falta; allowlist de chat_id en variable de entorno evaluada ANTES de Bronze, fail-fast si falta/vacía — descartado explícitamente 'allowlist vacía = admito a todos'; sin gate de coste batch, rate-limit fuera de ronda; puerto 8443 justificado por que la Bot API solo acepta 443/80/88/8443 y Coolify ya ocupa 80/443) y §Orquestación end-to-end (tramo síncrono 1-6 vs background 7-11 vía BackgroundTasks sin cola externa; pre-limpieza va en el tramo síncrono, no en background, aclarando ambigüedad del título de la task 35; un duplicado no agenda el tramo background; nota de recuperación post-200 remitiendo a tasks 46/47 sin diseñarla, y de routing.py remitiendo a tasks 33/34 sin diseñar su interfaz). Añadida P22 a docs/specs/00-overview.md (header + cuerpo, estilo P19/P20/P21). Añadidas a .env.example: TELEGRAM_WEBHOOK_SECRET_TOKEN, TELEGRAM_ALLOWED_CHAT_IDS, TELEGRAM_WEBHOOK_URL. No se tocó telegram_bot.py (congelado, task 16) ni ningún código de historico/. Diff 100% documentación; 449 tests unitarios en verde (2 errores preexistentes ajenos: PyYAML ausente en el venv de este worktree para test_run_historico_workflow.py de la task 31, y fixtures de datos privados de test_whisperx_parser.py no presentes en el worktree — mismo hallazgo ya documentado en tasks 22/23/27/28/30/31). 0 rechazos de FISCAL. Branch task/32-spec-webhook-orquestacion-flujo-b, PR #30, merge commit 8fa611d (método --merge, no squash, precedente del repo).",
      "rejections": 0
    },
    {
      "id": 33,
      "title": "Contrato B/C: spec del routing IGUAL/CAMBIADO/NUEVO a Supabase como componente compartido (src/jokes/SPEC.md §Routing)",
      "status": "done",
      "summary": "Tarea de SPEC (sin código ni tests, patrón de tasks 24/29/32). Añadida a src/jokes/SPEC.md la sección §Routing (IGUAL/CAMBIADO/NUEVO a Supabase) entre §Reconciliación y §Limpieza: firma pública rutear_chiste(store, estructurado, recon, *, tipo_fuente, tema_id=None, tecnica_id=None) -> ResultadoRuteo en src/jokes/routing.py (keyword-only tras los 3 primeros — evita swap posicional silencioso de tema_id/tecnica_id); tabla de parámetros; las 3 ramas con comportamiento idéntico al de hoy (IGUAL no escribe; NUEVO=crear_chiste(v1)+crear_revision(v1); CAMBIADO=obtener_chiste→version+1→crear_revision+actualizar_chiste, sin sobrescribir tema_id/tecnica_id con None); traslado del helper _estructura_revision; tabla 'qué se extrae literalmente vs qué cambia'; contrato de regresión (tests de la task 27 no se tocan); tabla de consumidores (task 34 propio_historico, task 35 propio). Nota mínima en src/jokes/historico/SPEC.md subdividiendo la etapa 5 en 5a (se queda en Flujo C: listar_candidatos_reconciliacion) / 5b (delegada a routing.py). Decisión 1: listar_candidatos_reconciliacion se queda en cada caller, NO entra en routing.py — Flujo B escribe tipo_fuente='propio' pero compara candidatos contra 'propio_historico' (alcance de lectura y de escritura no coinciden), un único parámetro tipo_fuente en el módulo compartido cablearía mal esa asimetría. Decisión 2: inicio_localizado (bandera del Segmentador, solo Flujo C) queda FUERA del contrato compartido — _rutear_a_supabase nunca lo lee, solo lo copia a la salida; Flujo C lo reañade envolviendo el ResultadoRuteo devuelto (ChisteRuteado como subclase frozen de 5º campo), verificado con un probe de construcción/asdict/identidad de bool. Branch task/33-spec-routing-compartido, commit 6b22d40, PR #31, mergeado --merge → merge commit d7d4d29. 449 tests unitarios en verde, tests de las tasks 27/28 sin modificar. Nota operativa: gh pr merge devolvió un error post-merge ('main ya usado por worktree', checkout local del worktree secundario, no afecta al merge en GitHub, verificado MERGED) — rama remota task/33-spec-routing-compartido no se borró automáticamente por el mismo motivo, consistente con el resto de ramas task/* del repo.",
      "rejections": 0
    },
    {
      "id": 34,
      "title": "Contrato B/C: src/jokes/routing.py — extrae _rutear_a_supabase de historico/pipeline.py a un módulo compartido",
      "status": "done",
      "summary": "Implementado src/jokes/routing.py (nuevo, paso 5b compartido): ResultadoRuteo (decision/chiste_id/tema_id/tecnica_id), rutear_chiste(store, estructurado, recon, *, tipo_fuente, tema_id=None, tecnica_id=None) con tema_id/tecnica_id keyword-only, y _estructura_revision movido tal cual. Las 3 ramas IGUAL/CAMBIADO/NUEVO son extracción literal de _rutear_a_supabase (task 27), único cambio: TIPO_FUENTE pasó de constante de módulo a parámetro tipo_fuente. src/jokes/historico/pipeline.py: ChisteRuteado ahora subclase frozen de ResultadoRuteo (+inicio_localizado, único campo fuera del contrato compartido); _rutear_a_supabase reducido a ~15 líneas que delegan en rutear_chiste(..., tipo_fuente=TIPO_FUENTE) y envuelven el resultado en ChisteRuteado. TIPO_FUENTE se conserva (la sigue usando la etapa 5a, listar_candidatos_reconciliacion, que no se movió). Contrato de regresión verificado: tests/unit/jokes/historico/test_pipeline.py (task 27) y tests/unit/scripts/test_run_historico_cli.py (task 28) NO se tocaron (git diff --stat vacío) y siguen en verde. 14 tests nuevos en tests/unit/jokes/test_routing.py (3 ramas, no-sobrescritura de taxonomías en CAMBIADO, keyword-only, forma del dataclass, _estructura_revision). 281/281 en tests/unit/jokes/, 361/361 en la suite completa descontando 11 errores de colección preexistentes y ajenos (langdetect/markitdown en theory/utils, fixtures privadas de whisperx — ninguno en jokes/). Sin ambigüedad de diseño, spec de la task 33 seguida literal; src/jokes/SPEC.md no necesitó cambios. 0 rechazos de FISCAL. Branch task/34-routing-compartido, commit b3ba5aa, PR #32, merge commit 0c7c4f3 (--merge, no squash).",
      "rejections": 0
    },
    {
      "id": 46,
      "title": "Flujo B: schema.sql añade procesado_at (nullable) a chistes_telegram_bronze + spec de recuperación post-200",
      "status": "done",
      "summary": "Tarea pequeña de DDL + spec (sin código Python, sin tests — no hay lógica nueva que testear). src/jokes/schema.sql: columna procesado_at (timestamptz, nullable, sin default) en chistes_telegram_bronze, con comentario SQL explicando que es bookkeeping de si el pipeline completó, no dato del mensaje; NULL = Bronze insertado pero tramo caro aún en curso o falló. texto_raw y el resto de columnas existentes intactas. src/jokes/telegram/SPEC.md: sección nueva de primer nivel ## Recuperación de fallos (entre Orquestación end-to-end e Idempotencia y versionado): qué es procesado_at, quién la marca (telegram/pipeline.py, task 35, al completar el paso 11 con éxito — sin diseñar pipeline.py aquí), quién la consume (scripts/reprocesar_bronze_pendiente.py, task 47, selecciona WHERE procesado_at IS NULL y reutiliza la misma función de pipeline.py que usa el webhook, mismo punto de marcado), por qué se marca al final y no en Bronze (si se marcara antes, un fallo posterior nunca se detectaría), coherencia explícita con la nota que la task 32 dejó en Orquestación, y nota de que no hay reintentos automáticos en esta tarea (el reproceso es invocación manual/programada aparte, task 47). No se tocó ningún código Python ni se creó telegram/pipeline.py ni reprocesar_bronze_pendiente.py (fuera de scope). 0 rechazos de FISCAL. Branch task/46-procesado-at-recuperacion, commit 6d4544b, PR #33, merge commit e50d5ce (--merge, no squash).",
      "rejections": 0
    },
    {
      "id": 35,
      "title": "Flujo B: telegram/pipeline.py — orquestador importable",
      "status": "done",
      "summary": "Implementado src/jokes/telegram/pipeline.py con dos funciones públicas separadas (no monolítica), pensadas para dos consumidores distintos: procesar_update_sincrono(update, store, allowlist: frozenset) -> ResultadoSincrono (tramo síncrono, pasos 3-5: reutiliza telegram_bot._extraer_datos_mensaje, evalúa allowlist de chat_id ANTES de tocar Supabase, solo si pasa llama a telegram_bot.procesar_mensaje_telegram para Bronze+pre-limpieza; devuelve estado ∈ aceptado|duplicado|ignorado_no_texto|ignorado_no_autorizado + texto_limpio + fila_bronze_id, consumida por webhook_app.py task 36 antes del 200) y procesar_evento_background(texto_limpio, fila_bronze_id, store, ...) -> ResultadoBackground (pasos 7-11: Silver -> taxonomías(tema+técnica) -> reconciliación [candidatos 'propio_historico', NUNCA 'propio' — constante TIPO_FUENTE_CANDIDATOS separada, verificado contra §Obtención de candidatos] -> routing.rutear_chiste(tipo_fuente='propio') -> store.marcar_telegram_bronze_procesado; excepciones de 7-10 se capturan/loguean sin marcar procesado_at, reutilizable idéntica por el webhook y por scripts/reprocesar_bronze_pendiente.py task 47). Nuevo SupabaseStore.marcar_telegram_bronze_procesado(fila_id) + builder puro _build_telegram_bronze_procesado_payload(), UPDATE que solo toca procesado_at (test explícito de que texto_raw queda intacto). licencia no se pasa explícita en ningún punto — default de columna 'comercializable' en schema.sql ya resuelve §Salida, ni crear_chiste ni rutear_chiste aceptan ese parámetro (verificado, no inventado). telegram_bot.py/silver.py/taxonomias.py/reconciliacion.py/routing.py/historico/** intactos. 15 tests nuevos en tests/unit/jokes/telegram/test_pipeline.py + 6 en tests/unit/jokes/test_supabase_store.py. 302/302 en tests/unit/jokes/, 493/493 en la suite completa (2 errores preexistentes ajenos: fixtures whisperx). Sin ambigüedad de diseño sin resolver. 0 rechazos de FISCAL. Branch task/35-telegram-pipeline-orquestador, commit 1898bc6, PR #34, merge commit ff10dd5 (--merge, no squash).",
      "rejections": 0
    },
    {
      "id": 36,
      "title": "Flujo B: telegram/webhook_app.py — app FastAPI",
      "status": "done",
      "summary": "Implementado src/jokes/telegram/webhook_app.py, transporte HTTP puro del Flujo B. POST /telegram/webhook: valida X-Telegram-Bot-Api-Secret-Token con hmac.compare_digest (403 sin parsear cuerpo si falta/no coincide, sin loguear la petición); 400 si el cuerpo no es JSON válido; delega el tramo síncrono íntegro a pipeline.procesar_update_sincrono (task 35, no tocado); responde siempre 200 {ok:true, estado} para toda petición autenticada+válida; agenda pipeline.procesar_evento_background en BackgroundTasks SOLO si estado=='aceptado'. GET /health: sin auth, sin tocar store, 200 {status:ok}. Factory crear_app(store=None, secret_token=None, allowlist=None): argumento explícito gana, si se omite resuelve de TELEGRAM_WEBHOOK_SECRET_TOKEN/TELEGRAM_ALLOWED_CHAT_IDS/SupabaseStore() real, fail-fast (ConfiguracionInvalidaError) si faltan/vacías/no parseables, nunca fallback abierto. Decisión de diseño: deliberadamente SIN 'app = crear_app()' a nivel de módulo (eso obligaría a credenciales reales de Supabase solo para importar el fichero, rompiendo pytest) — producción se sirve con 'uvicorn ...webhook_app:crear_app --factory' (uvicorn invoca la factory una vez al arrancar, sigue siendo fail-fast real). Tests con store doble (FakeStore) y procesar_update_sincrono/procesar_evento_background monkeypatcheados a nivel de módulo para aislar transporte de la lógica ya aprobada. requirements.txt: añadidos fastapi, uvicorn, httpx (TestClient). pipeline.py/telegram_bot.py/silver.py/taxonomias.py/reconciliacion.py/routing.py/supabase_store.py/historico/** intactos (git diff --stat: solo requirements.txt + 2 ficheros nuevos). No se implementó scripts/set_telegram_webhook.py (task 37) ni Dockerfile/compose (task 38). 16 tests nuevos en tests/unit/jokes/telegram/test_webhook_app.py. 318/318 en tests/unit/jokes/, 509/509 en la suite completa (2 errores preexistentes ajenos: fixtures whisperx). 0 rechazos de FISCAL. Branch task/36-telegram-webhook-app, commit 8fd732e, PR #35, merge commit bb6b797 (--merge, no squash).",
      "rejections": 0
    }
  ]
}
